# Port mapping for host:container (host chooses)
PORT=8080

# Admin GUI/API bearer token (simple)
ADMIN_TOKEN=super-secret-admin-token

# SQLite DB path inside container
DATABASE_URL=sqlite:////srv/data/db/license.db

# Days an activation token remains valid for offline usage
ACTIVATION_TTL_DAYS=7
